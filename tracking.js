const statusLabels={new:"تم استلام الطلب",contacted:"تم التواصل",scheduled:"تم تحديد الموعد",in_progress:"جاري تنفيذ الخدمة",completed:"اكتملت الخدمة",cancelled:"الطلب ملغي"};
const escapeHtml=value=>String(value??"").replace(/[&<>'"]/g,character=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[character]);
document.getElementById("trackingForm").addEventListener("submit",async event=>{event.preventDefault();const data=new FormData(event.currentTarget);const status=document.getElementById("trackingStatus");const result=document.getElementById("trackingResult");status.textContent="جارٍ البحث...";result.innerHTML="";try{const response=await fetch(`/api/requests/track?ticket_code=${encodeURIComponent(data.get("ticket"))}&phone=${encodeURIComponent(data.get("phone"))}`);if(!response.ok){status.textContent=response.status===429?"محاولات كثيرة. انتظر قليلًا ثم حاول مجددًا.":"لم نجد طلبًا مطابقًا. تأكد من البيانات.";return;}const item=await response.json();status.textContent="";const safeStatus=Object.hasOwn(statusLabels,item.status)?item.status:"new";result.innerHTML=`<article class="tracking-card"><span class="status-badge status-${safeStatus}">${escapeHtml(statusLabels[safeStatus])}</span><h2>${escapeHtml(item.ticket_code)}</h2><p><strong>الخدمة:</strong> ${escapeHtml(item.service)}</p><p><strong>المدينة:</strong> ${escapeHtml(item.city)}</p><p><strong>آخر تحديث:</strong> ${escapeHtml(new Date(item.updated_at).toLocaleString("ar-SA"))}</p>${item.admin_note?`<div class="notice">${escapeHtml(item.admin_note)}</div>`:""}</article>`;}catch{status.textContent="تعذر الاتصال بالخدمة الآن. حاول مجددًا بعد قليل.";}});
